<p style="color: green"><%= notice %></p>
<div class='products d-flex flex-wrap' id="products">
  <% @products.each do |product| %>
    <%= render product %>
  <% end %>
</div>
<div class='container'>
  <%= link_to "New product", new_product_path, class: 'btn btn-success btn-sm new_product'  %>
</div>